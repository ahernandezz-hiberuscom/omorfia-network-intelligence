# Runtime for the Streamlit application and the full pipeline.
# Deliberately small: no ML framework, no geospatial stack beyond shapely,
# no LLM SDK (the model adapter uses urllib against plain HTTP APIs).

streamlit>=1.49          # width="stretch" requires 1.49+
folium>=0.16
shapely>=2.0
pandas>=2.0
altair>=5.0
matplotlib>=3.8
